const name = 'john';
const age = 25;
const sentence = "Hey it's me " + name + " and I'm " + age + "years old";

const value = `Hey it's me ${name} and I'm ${age} years old ` // backticks (``) instead of ("") and ${} instead of concatenation

console.log(value);
